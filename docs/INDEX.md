# Documentation Index

This file routes engineering work to the smallest useful documentation set. Always read `DEVELOPMENT_PLAN.md`, this index, and the relevant section of `ARCHITECTURE.md`. Do not read every document unless the task crosses those boundaries.

## Product and UI work

Read `PRODUCT.md`, `UI_UX.md`, and the feature-specific document. For chat UI, also read `PURE_CONVERSATION.md` and `CONTEXT_MANAGEMENT.md`.

## Chat, API, or model work

Read `PURE_CONVERSATION.md`, `API_PROTOCOL.md`, `CONTEXT_MANAGEMENT.md`, and `DATA_MODEL.md`.

## Concurrent generation work

Read `GENERATION_TASKS.md`, `ANDROID_BACKGROUND.md`, `DATA_MODEL.md`, and `PERFORMANCE.md`.

## API key or settings work

Read `SECURITY.md`, `API_PROTOCOL.md`, and `DATA_MODEL.md`.

## Database work

Read `DATA_MODEL.md`, `BACKUP_AND_RECOVERY.md`, and `PERFORMANCE.md`.

## Vision work

Read `API_PROTOCOL.md`, `SECURITY.md`, `UI_UX.md`, and `PERFORMANCE.md`.

## Background execution work

Read `ANDROID_BACKGROUND.md`, `GENERATION_TASKS.md`, `SECURITY.md`, and `DATA_MODEL.md`.

## Performance work

Read `PERFORMANCE.md`, `ARCHITECTURE.md`, and the feature-specific document.

## Export, trash, or recovery work

Read `BACKUP_AND_RECOVERY.md`, `DATA_MODEL.md`, and `SECURITY.md`.

## Testing work

Read `TESTING.md`, `PURE_CONVERSATION.md`, and the feature-specific document.

## Release work

Read the repository [README](../README.md), `TESTING.md`, `SECURITY.md`, `PURE_CONVERSATION.md`, `UI_UX.md`, and `BACKUP_AND_RECOVERY.md`.

## Local model work

Read `LOCAL_MODEL_TUNING.md`, `PURE_CONVERSATION.md`, `CONTEXT_MANAGEMENT.md`, `SECURITY.md`, and `API_PROTOCOL.md`.

## Decision records

Read `DECISIONS.md` when a task touches an established architectural choice. Add an entry only for an important, non-obvious decision; progress belongs in `DEVELOPMENT_PLAN.md`.
