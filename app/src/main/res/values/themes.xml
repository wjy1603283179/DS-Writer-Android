<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Light window theme. The Compose theme owns all in-app color and type; this stylesheet only
      configures the window itself so the frame matches the Compose surface instead of the
      platform default (white status bar over a tinted background, startup flash, and so on).
    -->
    <style name="Theme.DSWriter" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:colorBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
